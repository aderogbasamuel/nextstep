import Link from 'next/link'
import { ArrowUpRight, CalendarDays, CheckCircle2, Clock3, FileText, Plus, TrendingUp } from 'lucide-react'
import { AppShell, PageHeader, StatusBadge } from '@/components/app-shell'

const recent = [
  { title: 'Software Engineering Internship', type: 'Internship', match: '78%', status: 'Eligible', date: 'Today', tone: 'green' as const },
  { title: 'African Technology Scholarship', type: 'Scholarship', match: '92%', status: 'Eligible', date: 'Yesterday', tone: 'green' as const },
  { title: 'Student Innovation Grant', type: 'Grant', match: '54%', status: 'Needs review', date: '2 days ago', tone: 'amber' as const },
]

export default function Dashboard() { return <AppShell><main className="mx-auto max-w-7xl px-5 py-9 lg:px-10 lg:py-11"><PageHeader eyebrow="Monday, September 24, 2026" title="Good morning, Samuel" description="Here&apos;s what needs your attention today." /><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Analyses','24',FileText,'All opportunities'],['Eligible','17',CheckCircle2,'71% of analyses'],['Needs review','4',Clock3,'Worth a closer look'],['Deadlines','3',CalendarDays,'Next 30 days']].map(([label,value,Icon,detail]) => <div key={label as string} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><p className="text-sm font-medium text-slate-500">{label as string}</p><span className="grid size-8 place-items-center rounded-lg bg-slate-50 text-slate-500"><Icon className="size-4" /></span></div><p className="mt-5 text-3xl font-bold tracking-tight">{value as string}</p><p className="mt-1 text-xs text-slate-400">{detail as string}</p></div>)}</div><div className="mt-8 grid gap-6 xl:grid-cols-[1.05fr_1.7fr]"><section className="rounded-xl border border-slate-200 bg-white shadow-sm"><div className="flex items-start justify-between border-b border-slate-100 px-5 py-5"><div><h2 className="font-semibold">Upcoming deadlines</h2><p className="mt-1 text-xs text-slate-400">Stay ahead of your applications.</p></div><CalendarDays className="size-5 text-slate-400" /></div><div className="divide-y divide-slate-100">{[['Software Engineering Internship','Sep 30','6 days left'],['African Technology Scholarship','Oct 04','10 days left'],['Student Innovation Grant','Oct 18','24 days left']].map(([title,date,remaining], i) => <div key={title} className="flex items-center gap-3 px-5 py-4"><span className={`grid size-9 place-items-center rounded-lg text-xs font-bold ${i === 0 ? 'bg-rose-50 text-rose-600' : 'bg-blue-50 text-blue-600'}`}>{date.split(' ')[1]}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{title}</p><p className="mt-1 text-xs text-slate-400">{date}</p></div><span className="text-xs font-medium text-slate-500">{remaining}</span></div>)}</div><Link href="/analyses" className="flex items-center justify-center gap-1 border-t border-slate-100 px-5 py-4 text-xs font-semibold text-blue-600">View all analyses <ArrowUpRight className="size-3.5" /></Link></section><section className="rounded-xl border border-slate-200 bg-white shadow-sm"><div className="flex items-start justify-between border-b border-slate-100 px-5 py-5"><div><h2 className="font-semibold">Recent analyses</h2><p className="mt-1 text-xs text-slate-400">Your latest opportunity reviews.</p></div><Link href="/analyses" className="text-xs font-semibold text-blue-600">See all</Link></div><div className="divide-y divide-slate-100">{recent.map((item) => <Link href="/analyses/1" key={item.title} className="flex items-center gap-4 px-5 py-4 transition hover:bg-slate-50"><span className="grid size-9 place-items-center rounded-lg bg-blue-50 text-blue-600"><FileText className="size-4" /></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{item.title}</p><p className="mt-1 text-xs text-slate-400">{item.type} · {item.date}</p></div><div className="hidden items-center gap-4 sm:flex"><span className="text-sm font-bold text-slate-700">{item.match}</span><StatusBadge tone={item.tone}>{item.status}</StatusBadge></div><ArrowUpRight className="size-4 text-slate-300" /></Link>)}</div></section></div><section className="mt-6 flex flex-col items-start justify-between gap-5 rounded-xl bg-slate-950 px-6 py-6 text-white sm:flex-row sm:items-center"><div><div className="flex items-center gap-2"><TrendingUp className="size-4 text-blue-300" /><p className="text-sm font-semibold">Your profile match is improving</p></div><p className="mt-1 text-sm text-slate-400">Add your latest experience to get more accurate results.</p></div><Link href="/profile" className="shrink-0 rounded-lg bg-white px-4 py-2.5 text-sm font-semibold text-slate-950">Update profile</Link></section><Link href="/analyze" className="mt-8 flex min-h-32 flex-col items-center justify-center rounded-xl border border-dashed border-slate-300 bg-white text-center transition hover:border-blue-400 hover:bg-blue-50/30"><span className="grid size-9 place-items-center rounded-full bg-blue-50 text-blue-600"><Plus className="size-4" /></span><span className="mt-3 text-sm font-semibold">Analyze a new opportunity</span><span className="mt-1 text-xs text-slate-400">Upload a document or paste a description</span></Link></main></AppShell> }
